#!/usr/bin/env bash

# This script runs the command 'whoami' as the specified user
# It takes one argument: the username
# If no argument is given, it prints the usage and exits with status 1

[ $# -eq 0 ] && { echo "Usage: $0 <username>"; exit 1; }
sudo -u "$1" whoami

